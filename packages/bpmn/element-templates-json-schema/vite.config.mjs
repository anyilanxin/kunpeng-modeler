import { defineConfig } from 'vite';

// This package ships JSON schema resources only; no compilation needed.
// Vite config is declared for tooling uniformity across the monorepo.
export default defineConfig({});
